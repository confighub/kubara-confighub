apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "InstallGate"
metadata:
  name: "cert-manager-default-r001"
spec:
  variantRevision: "../variant-revision.yaml"
  renderedObjectSetSHA256: "9e9b6442f769b8a02974ae801946207452f7f4990e873e007e4d32ec88d9cc09"
  decision: "blocked"
  allowedScopes:
    - "offline-local-evaluation"
  blockedScopes:
    - "live-cluster"
    - "root-catalog-promotion"
    - "production"
  reasons:
    - "Helm equivalence passed for default"
    - "CRD install/upgrade behavior needs explicit lifecycle policy before production"
    - "Admission webhook availability needs a fresh observation receipt after apply"
    - "Helm startup API check hook Job needs explicit lifecycle policy before production"
    - "Cluster-scoped RBAC needs production review"
    - "requires the six cert-manager CRDs from the reviewed crds-enabled base before applying webhook objects; excludes the Helm startup API check hook Job from the rendered revision"
    - "live qualification has not run"
    - "Kubara ServiceDefinition, wrapper, defaults, and additions compatibility is incomplete"
