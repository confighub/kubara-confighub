apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "InstallGate"
metadata:
  name: "oauth2-proxy-default-r001"
spec:
  variantRevision: "../variant-revision.yaml"
  renderedObjectSetSHA256: "1e6b5312d0e3ec8d8c0e306fb995604d377c89207409e35f2934f3526ac54abc"
  decision: "warn"
  allowedScopes:
    - "catalog-selection"
    - "local-test"
  blockedScopes:
    - "production"
  reasons:
    - "Helm equivalence passed for default"
    - "the exact artifact and deterministic installer package are retained for Kubara selection"
    - "Kubara wrapper behavior and target-specific live readiness require separate review"
    - "target facts and lifecycle prerequisites remain explicit and target-specific"
