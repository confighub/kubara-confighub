# Kind-only target fact. A real platform points this name at its external backend.
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: hx-app-dev-dev
spec:
  provider:
    fake:
      data:
        - key: hx-app-dev/dev/kube-prometheus-stack/grafana_credentials
          value: '{"admin-user":"admin","admin-password":"admin"}'
