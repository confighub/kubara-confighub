# Kind proof override: use a deterministic local issuer instead of public ACME.
letsencrypt: false
clusterIssuer:
  customDefinition:
    apiVersion: cert-manager.io/v1
    kind: ClusterIssuer
    metadata:
      name: selfsigned-root-issuer
    spec:
      selfSigned: {}
